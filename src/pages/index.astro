---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import mePhoto from '../assets/me.png';
---

<BaseLayout title="Home">
  <div class="container">
    <section class="intro">
      <h1>About me</h1>
      <div class="content">
        <div class="profile-image">
          <Image src={mePhoto} alt="Jake Wilkins"/>
        </div>
        <div class="bio">
          <p>
            Founding engineer with a love for building applications within the venn diagram of climate, geospatial and distributed technology.
            I'm currently obssessed with the intersection of Earth Engine, Xarray, Dask and Icechunk for distributed computing of
            EO data. Trying to work out how to serve that for tropical soft-commodity supply chains at <a href="https://epoch.blue" target="_blank">Epoch</a>.
          </p>
          <p>You can find me on <a href="https://www.linkedin.com/in/jake-w-wilkins/" target="_blank">LinkedIn</a> and <a href="https://github.com/jakenotjay" target="_blank">Github</a>.</p>
        </div>
      </div>
      <h2>This site and the use of gen AI</h2>
      <div class="content">
        <p>
          This site is built with Astro, deployed on github pages, and was majority written with Cursor and Claude.
        </p>
        <p>
          My writing has and always will be my own.
        </p>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .intro h1 {
    margin-bottom: var(--space-lg);
  }
  .intro h2 {
    margin-bottom: var(--space-md);
  }

  .content {
    margin-bottom: var(--space-2xl);
  }

  .content:has(.profile-image) {
    display: flex;
    gap: var(--space-lg);
    align-items: flex-start;
    justify-content: flex-start;
  }

  .profile-image {
    width: 180px;
    height: 240px;
    flex-shrink: 0;
    overflow: hidden;
  }

  .profile-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .content p {
    color: var(--text-secondary);
    line-height: 1.7;
    margin: 0;
  }

  .bio {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .bio p {
    margin: 0;
  }

  .content a {
    color: var(--accent);
  }

  .content a:hover {
    color: var(--accent-hover);
  }

  @media (max-width: 640px) {
    .content {
      text-align: center;
    }

    .content:has(.profile-image) {
      flex-direction: column;
      align-items: center;
    }

    .profile-image {
      width: 100px;
      height: 100px;
    }
  }
</style>
